# Professor VPN v10

## Finish the batch, pin greens, tunnel the whole device

- **A 240-row list is walked before the next list is added.** Search no
  longer cuts after ~18 pings and then drops Server 2000 on top.
- **My Configs pins a green row the instant it pings.**
- **Connected now tunnels IPv4 and IPv6.** Instagram and other apps no
  longer leak outside the VPN.
- **AUTO uses MASTER** on mobile/Wi-Fi so MPTCP cannot look connected
  while the TUN is empty.
- **Search starts from the cached network profile** when it is still
  fresh. A number on screen is still a real e2e ping.

versionCode 81. Same signing key as v9.9. Universal APK.
